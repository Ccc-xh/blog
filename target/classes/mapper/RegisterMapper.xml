<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wzh.dao.RegisterMapper">
    <insert id="registerUser">
        INSERT INTO `user` (
		nickName,
		phone,
		username,
		password
)VALUES(
		#{user.nickName},
		#{user.phone},
		#{user.username},
		#{user.password}
)
    </insert>
</mapper>

